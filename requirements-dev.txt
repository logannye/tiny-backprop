# Core library (editable install)
-e .

# Testing & linting
pytest>=7.4
pytest-cov>=4.1
ruff>=0.5

# PyTorch (CPU by default; replace with CUDA wheels as needed)
torch>=2.3.0
torchvision>=0.18.0
psutil>=5.9

# JAX (CPU build; follow jax.dev for GPU wheels)
jax>=0.4.27
jaxlib>=0.4.27
pynvml>=11.0.0; platform_system == "Linux" or platform_system == "Windows"

# Misc utilities
numpy>=1.26
scipy>=1.11
tabulate>=0.9


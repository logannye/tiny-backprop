[pytest]
addopts = -ra --strict-markers
testpaths =
    tests/unit
    tests/integration
markers =
    gpu: requires CUDA-enabled hardware to run
    slow: exercises heavy models or long-running benchmarks
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning

